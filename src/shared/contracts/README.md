# Shared Contracts

Frontend-facing backend interfaces, workflow results, and events live here. Contracts may depend
on `shared/data`, but never on frontend, backend, bootstrap, React Native, Expo, or concrete
persistence and integration implementations.

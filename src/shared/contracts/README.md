# Shared Contracts

Frontend-facing interfaces, DTO schemas, workflow results, events, and errors live here. Contracts
may depend on shared domain types, but never on frontend, backend, bootstrap, React Native, Expo,
or concrete persistence and integration implementations.

# Shared Data API

Platform-neutral DTO schemas, pagination types, and data errors live here. These modules describe
the values that cross the in-process frontend/backend seam; they do not implement transport,
persistence, React Query, or HTTP handlers.

# Assistant Picker

This module owns reusable assistant selection UI.

## Public Interface

- `AssistantPickerBottomSheet` renders the assistant picker bottom sheet.

## Organization

- `components/` contains the picker UI.
- `index.ts` is the public import surface.

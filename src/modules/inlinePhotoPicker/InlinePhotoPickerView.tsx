import { View } from 'react-native';

import type { InlinePhotoPickerViewProps } from './InlinePhotoPickerView.types';

export function InlinePhotoPickerView({ style }: InlinePhotoPickerViewProps) {
  return <View style={style} />;
}

import { createIcon } from '../createIcon';

/**
 * TypeIcon — SF `textformat` / Material `text_fields` (was lucide `type`).
 */
export default createIcon({ displayName: 'TypeIcon', sf: 'textformat', glyph: '\ue262' });

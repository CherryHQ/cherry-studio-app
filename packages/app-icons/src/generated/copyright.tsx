import { createIcon } from '../createIcon';

/**
 * CopyrightIcon — SF `c.circle` / Material `copyright` (was lucide `copyright`).
 */
export default createIcon({ displayName: 'CopyrightIcon', sf: 'c.circle', glyph: '\ue90c' });

import { createIcon } from '../createIcon';

/**
 * EyeOffIcon — SF `eye.slash` / Material `visibility_off` (was lucide `eye-off`).
 */
export default createIcon({ displayName: 'EyeOffIcon', sf: 'eye.slash', glyph: '\ue8f5' });

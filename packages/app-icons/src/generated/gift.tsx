import { createIcon } from '../createIcon';

/**
 * GiftIcon — SF `gift` / Material `redeem` (was lucide `gift`).
 */
export default createIcon({ displayName: 'GiftIcon', sf: 'gift', glyph: '\ue8b1' });

import { createIcon } from '../createIcon';

/**
 * PaintbrushIcon — SF `paintbrush` on iOS, Material `brush` on Android.
 */
export default createIcon({ displayName: 'PaintbrushIcon', sf: 'paintbrush', glyph: '\ue3ae' });

import { createIcon } from '../createIcon';

/**
 * PersonaIcon — SF `person.2` on iOS, Material `groups` on Android.
 */
export default createIcon({ displayName: 'PersonaIcon', sf: 'person.2', glyph: '\uf233' });

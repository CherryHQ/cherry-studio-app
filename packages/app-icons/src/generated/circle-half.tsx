import { createIcon } from '../createIcon';

/**
 * CircleHalfIcon — SF `circle.lefthalf.filled` on iOS, Material `contrast` on Android.
 */
export default createIcon({
  displayName: 'CircleHalfIcon',
  sf: 'circle.lefthalf.filled',
  glyph: '\ueb37',
});

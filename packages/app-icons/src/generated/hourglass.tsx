import { createIcon } from '../createIcon';

/**
 * HourglassIcon — SF `hourglass` on iOS, Material `hourglass_top` on Android.
 */
export default createIcon({ displayName: 'HourglassIcon', sf: 'hourglass', glyph: '\uea5b' });

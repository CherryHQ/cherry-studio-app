import { createIcon } from '../createIcon';

/**
 * CalendarIcon — SF `calendar` / Material `calendar_today` (was lucide `calendar`).
 */
export default createIcon({ displayName: 'CalendarIcon', sf: 'calendar', glyph: '\ue935' });

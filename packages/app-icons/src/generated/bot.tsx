import { createIcon } from '../createIcon';

/**
 * BotIcon — SF `brain.head.profile` on iOS, Material `smart_toy` on Android.
 */
export default createIcon({ displayName: 'BotIcon', sf: 'brain.head.profile', glyph: '\uf06c' });

import { createIcon } from '../createIcon';

/**
 * LightbulbIcon — SF `lightbulb` / Material `lightbulb` (was lucide `lightbulb`).
 */
export default createIcon({ displayName: 'LightbulbIcon', sf: 'lightbulb', glyph: '\ue0f0' });

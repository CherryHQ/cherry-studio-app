import { createIcon } from '../createIcon';

/**
 * MessageCircleIcon — SF `message` / Material `chat_bubble` (was lucide `message-circle`).
 */
export default createIcon({ displayName: 'MessageCircleIcon', sf: 'message', glyph: '\ue0ca' });

import { createIcon } from '../createIcon';

/**
 * GlobeIcon — SF `globe` on iOS, Material `public` on Android.
 */
export default createIcon({ displayName: 'GlobeIcon', sf: 'globe', glyph: '\ue80b' });

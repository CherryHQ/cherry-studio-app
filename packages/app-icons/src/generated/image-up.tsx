import { createIcon } from '../createIcon';

/**
 * ImageUpIcon — SF `photo.badge.plus` / Material `add_photo_alternate` (was lucide `image-up`).
 * DRIFT: Upload arrow becomes an add-photo badge on both platforms.
 */
export default createIcon({ displayName: 'ImageUpIcon', sf: 'photo.badge.plus', glyph: '\ue43e' });

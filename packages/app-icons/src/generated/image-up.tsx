import { createIcon } from '../createIcon';

/**
 * ImageUpIcon — SF `photo.badge.plus` on iOS, Material `add_photo_alternate` on Android.
 */
export default createIcon({ displayName: 'ImageUpIcon', sf: 'photo.badge.plus', glyph: '\ue43e' });

import { createIcon } from '../createIcon';

/**
 * BrainIcon — SF `brain.head.profile` on iOS, Material `psychology` on Android.
 */
export default createIcon({ displayName: 'BrainIcon', sf: 'brain.head.profile', glyph: '\uea4a' });

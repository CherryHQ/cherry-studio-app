import { createIcon } from '../createIcon';

/**
 * MonitorCloudIcon — SF `externaldrive.badge.icloud` on iOS, Material `backup` on Android.
 */
export default createIcon({
  displayName: 'MonitorCloudIcon',
  sf: 'externaldrive.badge.icloud',
  glyph: '\ue864',
});

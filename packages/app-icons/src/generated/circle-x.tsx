import { createIcon } from '../createIcon';

/**
 * CircleXIcon — SF `xmark.circle` on iOS, Material `cancel` on Android.
 */
export default createIcon({ displayName: 'CircleXIcon', sf: 'xmark.circle', glyph: '\ue5c9' });

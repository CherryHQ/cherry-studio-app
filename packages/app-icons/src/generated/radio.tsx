import { createIcon } from '../createIcon';

/**
 * RadioIcon — SF `dot.radiowaves.up.forward` on iOS, Material `radio` on Android.
 */
export default createIcon({
  displayName: 'RadioIcon',
  sf: 'dot.radiowaves.up.forward',
  glyph: '\ue03e',
});

import { createIcon } from '../createIcon';

/**
 * EllipsisIcon — SF `ellipsis` / Material `more_horiz` (was lucide `ellipsis`).
 */
export default createIcon({ displayName: 'EllipsisIcon', sf: 'ellipsis', glyph: '\ue5d3' });

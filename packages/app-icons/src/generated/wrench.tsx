import { createIcon } from '../createIcon';

/**
 * WrenchIcon — SF `wrench.adjustable` / Material `build` (was lucide `wrench`).
 */
export default createIcon({ displayName: 'WrenchIcon', sf: 'wrench.adjustable', glyph: '\ue869' });

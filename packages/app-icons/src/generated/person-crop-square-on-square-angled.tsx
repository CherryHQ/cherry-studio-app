import { createIcon } from '../createIcon';

/**
 * PersonCropSquareOnSquareAngledIcon — SF `person.crop.square.on.square.angled` on iOS, Material `recent_actors` on Android.
 */
export default createIcon({
  displayName: 'PersonCropSquareOnSquareAngledIcon',
  sf: 'person.crop.square.on.square.angled',
  glyph: '\ue03f',
});

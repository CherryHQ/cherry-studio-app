import { createIcon } from '../createIcon';

/**
 * ImagesIcon — SF `photo.on.rectangle.angled` on iOS, Material `photo_library` on Android.
 */
export default createIcon({
  displayName: 'ImagesIcon',
  sf: 'photo.on.rectangle.angled',
  glyph: '\ue413',
});

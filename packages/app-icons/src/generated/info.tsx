import { createIcon } from '../createIcon';

/**
 * InfoIcon — SF `info.circle` on iOS, Material `info` on Android.
 */
export default createIcon({ displayName: 'InfoIcon', sf: 'info.circle', glyph: '\ue88e' });

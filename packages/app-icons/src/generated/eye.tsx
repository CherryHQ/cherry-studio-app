import { createIcon } from '../createIcon';

/**
 * EyeIcon — SF `eye` / Material `visibility` (was lucide `eye`).
 */
export default createIcon({ displayName: 'EyeIcon', sf: 'eye', glyph: '\ue8f4' });

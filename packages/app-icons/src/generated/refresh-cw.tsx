import { createIcon } from '../createIcon';

/**
 * RefreshCwIcon — SF `arrow.clockwise` / Material `refresh` (was lucide `refresh-cw`).
 */
export default createIcon({ displayName: 'RefreshCwIcon', sf: 'arrow.clockwise', glyph: '\ue5d5' });

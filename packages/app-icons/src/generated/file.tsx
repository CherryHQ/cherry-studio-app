import { createIcon } from '../createIcon';

/**
 * FileIcon — SF `doc` / Material `draft` (was lucide `file`).
 */
export default createIcon({ displayName: 'FileIcon', sf: 'doc', glyph: '\ue66d' });

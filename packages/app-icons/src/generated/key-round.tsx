import { createIcon } from '../createIcon';

/**
 * KeyRoundIcon — SF `key` / Material `key` (was lucide `key-round`).
 */
export default createIcon({ displayName: 'KeyRoundIcon', sf: 'key', glyph: '\ue73c' });

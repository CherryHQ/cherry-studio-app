import { createIcon } from '../createIcon';

/**
 * BellIcon — SF `bell` on iOS, Material `notifications` on Android.
 */
export default createIcon({ displayName: 'BellIcon', sf: 'bell', glyph: '\ue7f4' });

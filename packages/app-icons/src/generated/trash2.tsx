import { createIcon } from '../createIcon';

/**
 * Trash2Icon — SF `trash` on iOS, Material `delete` on Android.
 */
export default createIcon({ displayName: 'Trash2Icon', sf: 'trash', glyph: '\ue872' });

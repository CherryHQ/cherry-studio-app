import { createIcon } from '../createIcon';

/**
 * ArrowDownIcon — SF `arrow.down` on iOS, Material `arrow_downward` on Android.
 */
export default createIcon({ displayName: 'ArrowDownIcon', sf: 'arrow.down', glyph: '\ue5db' });

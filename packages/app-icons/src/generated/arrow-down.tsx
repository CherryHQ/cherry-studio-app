import { createIcon } from '../createIcon';

/**
 * ArrowDownIcon — SF `arrow.down` / Material `arrow_downward` (was lucide `arrow-down`).
 */
export default createIcon({ displayName: 'ArrowDownIcon', sf: 'arrow.down', glyph: '\ue5db' });

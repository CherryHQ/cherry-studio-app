import { createIcon } from '../createIcon';

/**
 * SparklesIcon — SF `sparkles` / Material `auto_awesome` (was lucide `sparkles`).
 */
export default createIcon({ displayName: 'SparklesIcon', sf: 'sparkles', glyph: '\ue65f' });

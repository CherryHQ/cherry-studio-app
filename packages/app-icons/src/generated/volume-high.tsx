import { createIcon } from '../createIcon';

/**
 * VolumeHighIcon — SF `speaker.wave.2.fill` on iOS, Material `volume_up` on Android.
 */
export default createIcon({
  displayName: 'VolumeHighIcon',
  sf: 'speaker.wave.2.fill',
  glyph: '\ue050',
});

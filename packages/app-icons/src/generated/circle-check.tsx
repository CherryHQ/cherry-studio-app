import { createIcon } from '../createIcon';

/**
 * CircleCheckIcon — SF `checkmark.circle` on iOS, Material `check_circle` on Android.
 */
export default createIcon({
  displayName: 'CircleCheckIcon',
  sf: 'checkmark.circle',
  glyph: '\ue86c',
});

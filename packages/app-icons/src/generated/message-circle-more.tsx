import { createIcon } from '../createIcon';

/**
 * MessageCircleMoreIcon — SF `ellipsis.bubble` on iOS, Material `more_horiz` on Android.
 */
export default createIcon({
  displayName: 'MessageCircleMoreIcon',
  sf: 'ellipsis.bubble',
  glyph: '\ue5d3',
});

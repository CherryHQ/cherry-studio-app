import { createIcon } from '../createIcon';

/**
 * MailIcon — SF `envelope` / Material `mail` (was lucide `mail`).
 */
export default createIcon({ displayName: 'MailIcon', sf: 'envelope', glyph: '\ue158' });

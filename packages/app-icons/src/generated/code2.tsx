import { createIcon } from '../createIcon';

/**
 * Code2Icon — SF `chevron.left.forwardslash.chevron.right` on iOS, Material `code` on Android.
 */
export default createIcon({
  displayName: 'Code2Icon',
  sf: 'chevron.left.forwardslash.chevron.right',
  glyph: '\ue86f',
});

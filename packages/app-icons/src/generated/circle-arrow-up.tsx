import { createIcon } from '../createIcon';

/**
 * CircleArrowUpIcon — SF `arrow.up.circle` on iOS, Material `arrow_circle_up` on Android.
 */
export default createIcon({
  displayName: 'CircleArrowUpIcon',
  sf: 'arrow.up.circle',
  glyph: '\uf182',
});

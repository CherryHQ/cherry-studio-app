import { createIcon } from '../createIcon';

/**
 * CircleArrowUpIcon — SF `arrow.up.circle` / Material `arrow_circle_up` (was lucide `circle-arrow-up`).
 */
export default createIcon({
  displayName: 'CircleArrowUpIcon',
  sf: 'arrow.up.circle',
  glyph: '\uf182',
});

import { createIcon } from '../createIcon';

/**
 * ChevronLeftIcon — SF `chevron.left` on iOS, Material `chevron_left` on Android.
 */
export default createIcon({ displayName: 'ChevronLeftIcon', sf: 'chevron.left', glyph: '\ue5cb' });

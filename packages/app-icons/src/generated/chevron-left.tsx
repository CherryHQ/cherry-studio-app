import { createIcon } from '../createIcon';

/**
 * ChevronLeftIcon — SF `chevron.left` / Material `chevron_left` (was lucide `chevron-left`).
 */
export default createIcon({ displayName: 'ChevronLeftIcon', sf: 'chevron.left', glyph: '\ue5cb' });

import { createIcon } from '../createIcon';

/**
 * ChevronUpIcon — SF `chevron.up` on iOS, Material `keyboard_arrow_up` on Android.
 */
export default createIcon({ displayName: 'ChevronUpIcon', sf: 'chevron.up', glyph: '\ue316' });

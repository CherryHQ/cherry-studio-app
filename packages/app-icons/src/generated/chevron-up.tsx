import { createIcon } from '../createIcon';

/**
 * ChevronUpIcon — SF `chevron.up` / Material `keyboard_arrow_up` (was lucide `chevron-up`).
 */
export default createIcon({ displayName: 'ChevronUpIcon', sf: 'chevron.up', glyph: '\ue316' });

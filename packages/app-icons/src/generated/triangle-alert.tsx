import { createIcon } from '../createIcon';

/**
 * TriangleAlertIcon — SF `exclamationmark.triangle` on iOS, Material `warning` on Android.
 */
export default createIcon({
  displayName: 'TriangleAlertIcon',
  sf: 'exclamationmark.triangle',
  glyph: '\ue002',
});

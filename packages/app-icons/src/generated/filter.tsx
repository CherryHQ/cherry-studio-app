import { createIcon } from '../createIcon';

/**
 * FilterIcon — SF `line.3.horizontal.decrease` on iOS, Material `filter_list` on Android.
 */
export default createIcon({
  displayName: 'FilterIcon',
  sf: 'line.3.horizontal.decrease',
  glyph: '\ue152',
});

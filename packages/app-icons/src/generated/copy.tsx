import { createIcon } from '../createIcon';

/**
 * CopyIcon — SF `doc.on.doc` / Material `content_copy` (was lucide `copy`).
 */
export default createIcon({ displayName: 'CopyIcon', sf: 'doc.on.doc', glyph: '\ue14d' });

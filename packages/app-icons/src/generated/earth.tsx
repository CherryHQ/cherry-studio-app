import { createIcon } from '../createIcon';

/**
 * EarthIcon — SF `globe.americas` / Material `globe` (was lucide `earth`).
 */
export default createIcon({ displayName: 'EarthIcon', sf: 'globe.americas', glyph: '\ue64c' });

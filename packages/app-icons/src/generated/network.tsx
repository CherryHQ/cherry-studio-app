import { createIcon } from '../createIcon';

/**
 * NetworkIcon — SF `network` on iOS, Material `network_node` on Android.
 */
export default createIcon({ displayName: 'NetworkIcon', sf: 'network', glyph: '\uf56e' });

import { createIcon } from '../createIcon';

/**
 * ArrowUpIcon — SF `arrow.up` on iOS, Material `arrow_upward` on Android.
 */
export default createIcon({ displayName: 'ArrowUpIcon', sf: 'arrow.up', glyph: '\ue5d8' });

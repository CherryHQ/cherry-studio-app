import { createIcon } from '../createIcon';

/**
 * MenuIcon — SF `line.3.horizontal` on iOS, Material `menu` on Android.
 */
export default createIcon({ displayName: 'MenuIcon', sf: 'line.3.horizontal', glyph: '\ue5d2' });

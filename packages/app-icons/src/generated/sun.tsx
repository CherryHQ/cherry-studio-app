import { createIcon } from '../createIcon';

/**
 * SunIcon — SF `sun.max` / Material `light_mode` (was lucide `sun`).
 */
export default createIcon({ displayName: 'SunIcon', sf: 'sun.max', glyph: '\ue518' });

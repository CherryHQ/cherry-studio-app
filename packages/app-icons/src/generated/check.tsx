import { createIcon } from '../createIcon';

/**
 * CheckIcon — SF `checkmark` / Material `check` (was lucide `check`).
 */
export default createIcon({ displayName: 'CheckIcon', sf: 'checkmark', glyph: '\ue5ca' });

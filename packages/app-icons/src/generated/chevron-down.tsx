import { createIcon } from '../createIcon';

/**
 * ChevronDownIcon — SF `chevron.down` / Material `keyboard_arrow_down` (was lucide `chevron-down`).
 */
export default createIcon({ displayName: 'ChevronDownIcon', sf: 'chevron.down', glyph: '\ue313' });

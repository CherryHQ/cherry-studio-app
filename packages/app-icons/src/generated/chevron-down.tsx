import { createIcon } from '../createIcon';

/**
 * ChevronDownIcon — SF `chevron.down` on iOS, Material `keyboard_arrow_down` on Android.
 */
export default createIcon({ displayName: 'ChevronDownIcon', sf: 'chevron.down', glyph: '\ue313' });

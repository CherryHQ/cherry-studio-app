import { createIcon } from '../createIcon';

/**
 * MessageCircleWarningIcon — SF `exclamationmark.bubble` on iOS, Material `chat_error` on Android.
 */
export default createIcon({
  displayName: 'MessageCircleWarningIcon',
  sf: 'exclamationmark.bubble',
  glyph: '\uf7ac',
});

import { createIcon } from '../createIcon';

/**
 * MapPinIcon — SF `mappin` / Material `location_on` (was lucide `map-pin`).
 */
export default createIcon({ displayName: 'MapPinIcon', sf: 'mappin', glyph: '\ue0c8' });

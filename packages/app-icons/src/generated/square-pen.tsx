import { createIcon } from '../createIcon';

/**
 * SquarePenIcon — SF `square.and.pencil` on iOS, Material `edit_square` on Android.
 */
export default createIcon({
  displayName: 'SquarePenIcon',
  sf: 'square.and.pencil',
  glyph: '\uf88d',
});

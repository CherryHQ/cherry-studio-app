import { createIcon } from '../createIcon';

/**
 * SquarePenIcon — SF `square.and.pencil` / Material `edit_square` (was lucide `square-pen`).
 */
export default createIcon({
  displayName: 'SquarePenIcon',
  sf: 'square.and.pencil',
  glyph: '\uf88d',
});
